#if !defined GL_VIEWPORT_SIZE
#define GL_VIEWPORT_SIZE

const int GL_VIEWPORT_LEFT   = (int)(20.0 / 25.4 * 72.0) ;
const int GL_VIEWPORT_BOTTOM = GL_VIEWPORT_LEFT ;
const int GL_VIEWPORT_WIDTH  = (int)(170.0 / 25.4 * 72.0) ;
const int GL_VIEWPORT_HEIGHT = (int)(257.0 / 25.4 * 72.0) ;
const double GL_VIEWPORT_ASPECT = 170.0 / 254.0 ;

#endif


